<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bridginggood"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="8" />

    <permission
        android:name="com.bridginggood.permission.C2D_MESSAGE"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.bridginggood.permission.C2D_MESSAGE" />
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:icon="@drawable/icon"
        android:label="@string/app_name" >
        <activity
            android:name=".SplashController"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- C2DM Receiver Activity -->
        <receiver
            android:name=".C2DMReceiver"
            android:permission="com.google.android.c2dm.permission.SEND" >
            <intent-filter>
                <action android:name="com.google.android.c2dm.intent.REGISTRATION" >
                </action>

                <category android:name="com.bridginggood" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.google.android.c2dm.intent.RECEIVE" >
                </action>

                <category android:name="com.bridginggood" />
            </intent-filter>
        </receiver>

        <!-- Main TabHost -->
        <activity
            android:name=".MainController"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.TabTheme" />

        <!-- Login -->
        <activity
            android:name=".LoginController"
            android:screenOrientation="portrait" />
        <activity
            android:name=".SignupController"
            android:screenOrientation="portrait" />

        <!-- Charity -->
        <activity
            android:name=".Charity.CharityActivityGroup"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Charity.CharityCurationController"
            android:screenOrientation="portrait" />

        <!-- Biz -->
        <activity
            android:name=".Biz.BizActivityGroup"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Biz.BizMapController"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Biz.BizListController"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Biz.BizDetailController"
            android:screenOrientation="portrait" />

        <!-- User -->
        <activity
            android:name=".User.UserActivityGroup"
            android:screenOrientation="portrait" />
        <activity
            android:name=".User.UserController"
            android:screenOrientation="portrait" />

        <!-- Bg -->
        <activity
            android:name=".Setting.SettingActivityGroup"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Setting.SettingController"
            android:screenOrientation="portrait" />

        <!-- Google Map -->
        <uses-library
            android:name="com.google.android.maps"
            android:required="true" />
    </application>

</manifest>